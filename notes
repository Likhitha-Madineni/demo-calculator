Directory - is nothing but a folders


ls - it lists all the files and folders in the directorty. 
cd - it changes directorty 
cd .. - it goes back to the directory 
pwd - tells you the directory path
clear - it clears the screen

git commands 

git init - it creates a local git repository ( it creates a git folder)
git add * - it adds all files into that git folder
git commit -m "msg" - it saves the work upto that point


git config --global user.email "2400033249@kluniversity.in"
git config --global user.name "Likhitha"


TO-BE-CONTINUED

